let simpleObject = {
    name: "luvuyo",
    birthDay: 15,
    black: true
};

// for (let keys in simpleObject) { //log the keys
//     console.log(keys);
// };

// for (let values in simpleObject) { //log the values
//     console.log(simpleObject[values]);
// };

let simpleArray = [
    "name",
    "birthDay",
    "black"
];

for (let keyz in simpleArray) { //log array elements using for in loop
    console.log(simpleArray[keyz]);
};
